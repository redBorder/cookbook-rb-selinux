---
driver:
  name: vagrant

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: rocky-9

suites:
  - name: default
    run_list:
      - recipe[rb-selinux::configure_solo]
    verifier:
      inspec_tests:
        - test/integration/default
    attributes:
